package org.brooth.jeta.samples.inject;

import org.brooth.jeta.inject.Module;

/**
 * @author Oleg Khalidov (brooth@gmail.com)
 */
@Module(scopes = SampleScope.class)
public interface SampleModule {
}
